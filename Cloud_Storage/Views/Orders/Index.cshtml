@model IEnumerable<Cloud_Storage.Models.Order>

<h1 class="text-center my-4 text-white">All Orders</h1>

<div class="container mb-4">
    <div class="d-flex justify-content-between align-items-center">
        <a href="@Url.Action("Register", "Orders")" class="btn btn-primary">Add New Order</a>
    </div>
</div>

<div class="container bg-dark p-4 rounded">
    <table class="table table-striped table-bordered text-white">
        <thead class="thead-dark">
            <tr>
                <th scope="col">Customer ID</th>
                <th scope="col">Product ID</th>
                <th scope="col">Location</th>
                <th scope="col">Date</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (var order in Model)
                {
                    <tr>
                        <td>@order.Customer_ID</td>
                        <td>@order.Product_ID</td>
                        <td>@order.Order_Location</td>
                        <td>@order.Order_Date.ToString("g")</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4" class="text-center">No orders found.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
